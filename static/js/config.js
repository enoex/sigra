/**
 * App config settings
 *
 *
 * @module config.js
 */

export default {
    // Use redux dev tools? If true, will slow things down considerably -
    // useful only for debugging / dev
    USE_DEV_TOOLS: true
};
